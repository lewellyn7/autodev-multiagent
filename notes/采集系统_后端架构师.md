# 后端架构梳理

## 技术栈

### Web 框架
- **FastAPI** — 主 Web 框架，提供 REST API + 页面渲染
- **Uvicorn** — ASGI 服务器（支持 reload 开发和多 worker 部署）
- **Jinja2Templates** — 服务端页面渲染（HTML templates）

### 爬虫核心
- **Playwright + playwright-stealth** — 浏览器自动化（渲染 JS 页面、反检测）
- **httpx / aiohttp** — 异步 HTTP 客户端（轻量页面）

### 数据持久化（双库）
- **PostgreSQL 16 + pgvector** — 主数据库，存储采集记录、用户、项目
- **SQLite** — 轻量配置/缓存存储（`tender_scraper.db`）
- **asyncpg** — PostgreSQL 异步驱动（连接池）
- **psycopg2-binary** — PostgreSQL 同步驱动（SQLite 降级路径）
- **Redis 7** — 缓存池（URL 去重、限频、LRU）

### 调度系统
- **APScheduler (BlockingScheduler)** — 独立容器运行，定时触发采集任务（08:00 / 12:00 / 18:00）
- **SmartScheduler** — 内存中动态优先级调度器（5 因子动态评分：来源可信度、截止时间接近度、历史成功率、请求间隔、关键词匹配）

### AI / NLP
- **RAGFlow** — 知识库 + RAG 查询（向量检索）
- **LLM Service** — 大模型摘要/分类（httpx 调用 vLLM HTTP API）
- **jieba** — 中文分词（关键词匹配）
- **numpy** — 向量计算（pgvector + TF-IDF）

### 监控与日志
- **Loguru** — 结构化日志（文件轮转）
- **Prometheus Client** — 指标暴露（`/metrics`）
- **Audit Log** — 安全审计（采集开始/成功/失败事件）

### 其他
- **Pydantic v2** — 数据验证与序列化
- **Pydantic Settings** — 环境变量配置管理
- **bcrypt** — 密码哈希
- **pytest + pytest-asyncio** — 测试框架
- **Docker + docker-compose** — 容器化部署

---

## 目录结构

```
tender-scraper/
├── main.py                     # 采集任务主入口（asyncio.run(run_collection())）
├── web_server.py               # FastAPI Web 服务入口
├── scheduler.py                # APScheduler 调度器入口（独立容器）
├── ccgp_main.py / harvest_main.py  # 旧版采集入口（已废弃）
├── quick_ccgp.py               # CCGP 快速采集脚本
│
├── app/
│   ├── api/
│   │   ├── routes.py           # 页面渲染路由（/dashboard、/data、/settings...）
│   │   ├── harvest_api.py      # 采集系统 REST API（/crawl、/status、/results）
│   │   ├── routes/             # 分模块 REST API 路由
│   │   │   ├── projects.py     # 项目 CRUD
│   │   │   ├── favorites.py    # 收藏
│   │   │   ├── stats.py        # 统计
│   │   │   ├── search.py       # 搜索
│   │   │   ├── chat.py         # AI 对话
│   │   │   ├── vector_search.py
│   │   │   ├── quality.py      # 质量评估
│   │   │   ├── tasks.py        # 任务管理
│   │   │   └── ... (20+ 模块)
│   │   ├── dependencies.py     # 路由依赖注入
│   │   ├── metrics.py          # Prometheus 中间件 + 指标端点
│   │   └── routes_users.py     # 用户认证（/login、/logout）
│   │
│   ├── core/
│   │   ├── browser.py          # StealthBrowser 浏览器管理（Playwright 封装）
│   │   ├── tender_crawler.py   # 爬虫能力配置（网站列表、选择器策略）
│   │   ├── harvest/             # 采集核心子系统
│   │   │   ├── smart_scheduler.py    # SmartScheduler（5因子动态优先级引擎）
│   │   │   ├── predictive_anomaly.py # 异常预测
│   │   │   ├── anti_detect/           # 反检测（TLS 指纹、Canvas、WebGL、DNS）
│   │   │   ├── human_behavior_engine.py  # 人类行为模拟
│   │   │   ├── cache_manager.py       # Redis 缓存管理
│   │   │   └── config.py
│   │   ├── session_memory.py    # LLM Session Memory（上下文窗口管理）
│   │   ├── priority_scheduler.py
│   │   ├── predictive_scaler.py
│   │   └── logging_config.py
│   │
│   ├── crawlers/
│   │   ├── base.py             # BaseCrawler 抽象基类（通用字段提取、URL 去重、重试）
│   │   ├── ccgp.py             # 中国政府采购网爬虫（aiohttp）
│   │   ├── cqggzy.py           # 重庆公共资源交易网爬虫（Playwright）
│   │   └── async_base.py       # AsyncHumanCrawlerBase（异步版本）
│   │
│   ├── database/
│   │   ├── db.py               # DatabaseManager（asyncpg 连接池 + SQLite 降级）
│   │   ├── async_models.py     # HarvestRecord 等 ORM 类（asyncpg 直接查询）
│   │   ├── tables/             # SQLite 表 Mixin（favorites、annotations、qualifications、users、keywords）
│   │   └── repositories/        # Repository 模式（annotation、backup、cache、duplicate、favorite、log、preset、project、user）
│   │
│   ├── models/
│   │   ├── tender.py           # TenderInfo（25字段）、ContactInfo、TenderAttachment
│   │   └── schemas.py          # Pydantic 模型（User、Project、Login 等）
│   │
│   ├── services/
│   │   ├── llm_service.py      # LLM 摘要/分类服务
│   │   ├── vector_store.py     # 向量存储（pgvector）
│   │   ├── qualification_matcher.py  # 投标人资质匹配
│   │   ├── quality_evaluation.py     # 质量评估
│   │   ├── recommendation_service.py  # 推荐服务
│   │   ├── document_analyzer.py
│   │   ├── keywords_service.py
│   │   ├── project_service.py
│   │   └── health_monitor.py
│   │
│   ├── nlp/
│   │   ├── classifier.py       # 文本分类
│   │   └── summarizer.py       # 摘要生成
│   │
│   ├── utils/
│   │   ├── filter.py           # TenderFilter（关键词过滤）
│   │   ├── report.py           # ReportGenerator（Excel/JSON 导出）
│   │   ├── notifications.py    # 通知发送
│   │   ├── security.py         # 安全工具
│   │   ├── pdf_generator.py
│   │   ├── session.py          # Session 会话管理
│   │   └── tfidf_matcher.py   # TF-IDF 关键词匹配
│   │
│   ├── middleware/
│   │   ├── security.py         # HTTPS 强制、安全头、请求日志、限流
│   │   └── csrf.py             # CSRF 保护（已禁用）
│   │
│   ├── security/
│   │   └── audit.py            # 审计日志（crawl_started/completed/failed）
│   │
│   └── templates/               # Jinja2 HTML 模板（dashboard、data、settings、favorites、analytics、logs...）
│
├── services/
│   └── ragflow_service.py       # RAGFlow API 客户端（知识库管理）
│
├── config/
│   ├── settings.py              # Pydantic Settings 配置
│   └── tender_scraper.db       # SQLite 数据库文件
│
├── docker-compose.yml           # Web + Redis + Postgres + Scheduler 多容器
├── docker-compose.prod.yml      # 生产部署配置
├── Dockerfile
├── requirements.txt
└── pyproject.toml
```

---

## 核心模块

### `app/core/harvest/smart_scheduler.py` — 智能调度引擎
Phase 1 核心，替代了旧版简单的 `asyncio.gather`。

**5 因子动态优先级：**
1. `source_weight` — 来源可信度（cqggzy=1.0, ccgp=0.9）
2. `urgency_deadline` — 截止时间紧迫度（指数衰减，越接近越高）
3. `historical_success` — 该来源历史成功率
4. `adaptive_interval` — 距离上次访问的时间间隔（防封禁）
5. `keyword_match_score` — 关键词匹配得分

**自适应间隔管理：** `AdaptiveIntervalManager` 维护每个站点的度量数据（响应时间、成功率、ban 状态），动态调整采集间隔。

---

### `app/crawlers/base.py` — 爬虫基类
所有爬虫继承 `BaseCrawler`：
- `_mark_visited()` — 异步线程安全 URL 去重
- `_extract_field()` / `_extract_field_by_kw()` — 正则/关键词字段提取
- `_extract_contact_info()` / `_extract_attachments()` / `_extract_budget()` / `_extract_deadline()` — 通用详情提取
- `_parse_date()` / `_parse_datetime()` — 日期解析（支持中文格式）
- `fetch_details_batch()` — 并行批量采集（Semaphore 控制并发）
- `_fetch_with_retry()` — 指数退避重试

子类只需实现 `fetch_list()` 和 `fetch_detail()` 两个抽象方法。

---

### `app/core/browser.py` — StealthBrowser
Playwright 封装，反检测配置：
- 伪造 `navigator.webdriver`、`navigator.plugins`、`navigator.hardwareConcurrency`
- Canvas / WebGL 随机噪声
- 浏览器指纹随机化
- User-Agent 轮换
- 隐式随机等待（`random_delay`）

---

### `app/database/db.py` — 双库适配
- `USE_PG = DATABASE_URL.startswith("postgresql://")` 检测
- PostgreSQL：`psycopg2.ThreadedConnectionPool`（10 连接）
- SQLite：原生 `sqlite3`（配置/缓存降级）
- Repository 模式封装 CRUD

---

### `app/scheduler.py` — APScheduler 调度器
独立容器运行，与 Web 服务完全解耦：
- `BlockingScheduler`（阻塞模式，主进程运行）
- `CronTrigger(hour="08,12,18", minute="0")` — 每天 3 次
- `misfire_grace_time=3600` — 小时级容忍延迟
- 调用 `main.run_collection()` 执行采集
- 审计日志写入（crawl_started / completed / failed）

---

### `app/services/` — 业务服务层
| 服务 | 职责 |
|---|---|
| `llm_service` | 调用 vLLM HTTP API 做摘要/分类 |
| `vector_store` | pgvector 向量存储与检索 |
| `qualification_matcher` | 投标人资质与招标要求匹配 |
| `quality_evaluation` | 采集结果质量评分 |
| `recommendation_service` | 基于关键词/浏览历史的推荐 |
| `document_analyzer` | PDF/Word 文档解析与分析 |
| `keywords_service` | 关键词 CRUD 管理 |
| `health_monitor` | 系统健康检查 |

---

## 数据模型

### TenderInfo（25 字段）
```
核心：title, url, category, publish_date
来源：source_url, source_name
业务：business_type（政府采购/工程招投标）, info_type（采购意向/公告/结果）
内容：content_preview（300字）, full_content, attachments, contact_info
金额：budget, bid_amount
时间：deadline, opening_date
地区/行业：region, industry, tender_type
工程专用：project_overview, bidder_requirements, submission_deadline/submission_location
元数据：keywords_matched, scraped_at, scraped_by
```

### 数据库表（PostgreSQL）
- `harvest_records` — 采集记录（title, source_url, source_name, matched_keywords, raw_data, status, retry_count）
- `users` — 用户（username, password_hash, role, display_name）
- `projects` — 项目（project_url, title, status, keywords_matched）
- `favorites` / `annotations` / `keywords` — 业务扩展表
- `bidder_qualifications` — 投标人资质
- `filter_presets` / `logs` / `duplicates` / `cache` / `backup` — 系统表

### Pydantic Schemas
`UserRole(ADMIN/EDITOR/VIEWER)`、`ProjectStatus(PENDING/MATCHED/REJECTED/ARCHIVED)`、`RecordStatus(PENDING/PROCESSING/DONE/FAILED)` 等枚举贯穿 API 响应。

---

## API 设计

### 页面渲染路由（`routes.py`）
```
GET /              → dashboard.html
GET /content       → data.html
GET /settings      → settings.html
GET /favorites     → favorites.html
GET /analytics     → analytics.html
GET /logs          → logs.html
```

### 采集 API（`harvest_api.py`）
```
POST /crawl               — 触发采集任务
GET  /status/{task_id}    — 查询任务状态
GET  /results/{task_id}   — 获取采集结果
GET  /stats               — 统计信息
```

### 业务 REST API（20+ 路由模块，`routes/`）
| 路由 | 模块 |
|---|---|
| `/api/projects` | projects |
| `/api/favorites` | favorites |
| `/api/search` | search |
| `/api/stats` | stats |
| `/api/analytics` | analytics |
| `/api/chat` | chat（RAG 对话） |
| `/api/vector/search` | vector_search |
| `/api/quality` | quality |
| `/api/tasks` | tasks |
| `/api/keywords` | keywords |
| `/api/presets` | presets |
| `/api/notifications` | notifications |
| `/api/exports` | exports |
| `/api/health` | health |
| `/api/logs` | logs |
| `/api/duplicates` | duplicates |
| `/api/annotations` | annotations |
| `/api/crawler-configs` | crawler_configs |
| `/api/config-backup` | config_backup |
| `/api/document-upload` | document_upload |
| `/api/priority-scheduler` | priority_scheduler |
| `/api/predictive-scaler` | predictive_scaler |
| `/api/bidder-qualifications` | bidder_qualifications |
| `/api/qualification-categories` | qualification_categories |

### 用户认证（`routes_users.py`）
```
POST /login
POST /logout
```

### 中间件
- `HTTPSForceMiddleware` — 强制 HTTPS
- `SecurityHeadersMiddleware` — 安全响应头
- `RequestLoggingMiddleware` — 请求日志
- `PrometheusMiddleware` — Prometheus 指标
- `RateLimitMiddleware` — 限流（已临时禁用）

---

## 调度机制

### 定时采集（APScheduler）
```
触发时间：每天 08:00 / 12:00 / 18:00（Asia/Shanghai）
入口函数：scheduler.job_run_collection()
执行流程：
  1. 写审计日志 EVENT_CRAWL_STARTED
  2. asyncio.run(run_collection())  → main.py
     - 启动 StealthBrowser
     - 创建 CQGGZYCrawlerV2
     - 采集列表页 → 过滤关键词 → 批量采集详情页
     - TenderFilter 关键词匹配
     - 结果写入 output/latest.json
  3. 写审计日志 EVENT_CRAWL_COMPLETED / EVENT_CRAWL_FAILED
```

### 动态优先级调度（SmartScheduler）
运行在 `main.py` 的采集流程内部：
- 接收 `CrawlTask` 列表（包含静态优先级 + URL）
- 5 因子动态评分 → `priority_dynamic`
- 按优先级排序 → 依次调度执行
- `AdaptiveIntervalManager` 控制请求间隔（防止封禁）
- 失败重试（最多 3 次，指数退避）

---

## 开发进度

### ✅ 已完成 (2026-04-18)
- tasks.py 完全重构: `async def` + `Depends(get_current_user)` + `ThreadPoolExecutor`
- 移除本地重复的 `get_current_user_id()` / `get_db_conn()`
- Repository 模式封装数据访问

### ✅ 已完成 (2026-04-21)
- **Scheduler Bug Fix**: 删除 app/config.py（遮蔽 app/config/ 包），修复 `No module named 'config.settings'` 错误
  - 影响: scheduler 定时采集自 2026-04-20 起全部失败，现已修复
- **多字段查重**: 后端重写比对算法，5维度加权评分
- **全量向量入库脚本**: scripts/backfill_vectors.py，支持批量向量化入库

### ✅ 已完成 (2026-04-21 PM)
- **调度器解耦 (Redis Pub/Sub)**: Scheduler 和 Collector Worker 完全分离
  - Scheduler 发布到 `tender:collect:trigger` channel
  - Collector Worker 订阅并执行 `run_collection()`
  - 端到端测试通过 (65/159 匹配, 10.1s)

### ✅ 已完成 (2026-04-22)
- **PostgreSQL 全量迁移 Phase 1**: 
  - ✅ `scripts/init_pg.sql` 建表脚本（16表 + pgvector扩展）
  - ✅ `PGVectorBackend` 向量后端实现
  - ✅ ChromaDB → pgvector 数据迁移（59条向量，dim=2560）
  - ⚠️ 向量维度问题：ChromaDB 实际存 2560 维，需统一 embedding 模型

**进度: 96%**

---

## 架构优缺点

### 优点

1. **模块化清晰** — `crawlers/` 继承基类复用逻辑，`services/` 分离业务关注点，`repositories/` 封装数据访问
2. **反检测能力** — StealthBrowser + anti_detect 子系统（TLS 指纹、Canvas、WebGL、行为模拟）有效对抗网站反爬
3. **智能调度** — SmartScheduler 5 因子动态优先级，相比固定间隔采集更高效、更安全
4. **双库灵活** — PostgreSQL（主数据）+ SQLite（轻量配置），支持降级
5. **RAG 生态** — 向量搜索 + LLM 摘要 + 投标人资质匹配，构建了完整的 AI 分析 pipeline
6. **Docker 生产就绪** — docker-compose 定义了 Web + Redis + Postgres + Scheduler 四服务分离
7. **审计日志** — 采集开始/成功/失败事件完整记录，便于问题追溯

### 缺点

1. **调度器与 Web 耦合** — `scheduler.py` 直接 import `main.run_collection()`，虽然容器解耦了进程，但代码层面仍有直接依赖，scheduler 重构时应考虑调用独立的采集 API 而非直接 import
2. **双库维护成本** — PostgreSQL + SQLite 两套 ORM 并存，Repository 模式不够彻底，部分模块直接写 SQL 字符串（`db.py` 中有大量 `cur.execute(f"...")`）
3. **URL 去重局限性** — `BaseCrawler._visited_urls` 是内存 `Set`，分布式多实例部署时会失效；应迁移到 Redis Set
4. **async/sync 混用** — 部分模块用 `asyncpg`（async），部分用 `psycopg2`（sync），缺乏统一抽象；`BaseCrawler` 是 sync 方法在 async context 中被调用时存在风险
5. **Schema 膨胀** — `models/schemas.py` 单文件承载了所有 Pydantic 模型（用户/项目/收藏/通知...），超过 600 行，违反单一职责
6. **配置分散** — `.env`、`.env.example`、`.env.production`、`config/settings.py` 多处配置源，缺少统一的配置校验层
7. **测试覆盖率不足** — 项目有 `tests/` 目录但质量存疑，critical path（采集流程、调度）缺少集成测试
8. **Scheduler 单点** — `BlockingScheduler` 单进程，misfire 时虽有 1 小时容错但无高可用机制；如需精度应迁移到分布式调度（APScheduler + Redis 或 Celery）
9. **CORS/CSRF 配置** — CSRF middleware 被注释禁用，依赖 Session Token header 认证，安全性依赖实现细节而非框架保障

---

## 2026-06-11 07:30 重新评估

> 距 2026-04-24 优化收尾 48 天。系统已从"百条级"演进为"万条级"，架构本身保持稳定，主要演进了**采集器、数据质量、AI 集成**三条线。

### 🔴 关键架构决策

**1. 采集源收敛 (2026-06-02 决策)**
- `ENABLE_CCGP = False`（main.py 顶部硬编码）
- 原因：CCGP SPA 架构 + 3 个月 API 窗口限制 + 详情页 JS 拦截不稳定
- 现状：CCGP 表保留 58 条历史数据不再采集；CQGGZY 唯一活跃源
- 影响：main.py 注释 + 9 个分类并行（CQGGZY 工程建设 + 政府采购）

**2. 主入口演化**
- main.py: 296 → 551 行 (+86%)
- 新增 `_build_vector_text()`：6 字段拼接向量化（title/type/business_type/info_type/project_overview/bidder_requirements + content_preview）
- 新增来源均衡采样（detail_limit=30/类）
- 新增 24h 漏采回放（启动时优先补采过去 7d 空 content URL）

**3. CCGP Crawler 全面重写 (2026-05-29)**
- app/crawlers/ccgp.py: 24K, 524 行（已废弃但保留）
- `CCGPCrawlerV3` 类
- LIST_URLS 修复：`/info-notice/{type}-list` 格式
- DB 写入：`upsert_projects_ccgp([row])`（与 cqggzy 分离）
- 现状：停采，不影响

**4. CQGGZY Crawler 重大演进 (736 行)**
- 列表 API `sort=newid` 字段 1→0 修复反向排序 bug
- 详情页 H1 拼接：`eval_on_selector_all('h1', ...)` 强制包含 header 区项目编号
- `_extract_date_from_url()`：URL `/20251206/` 路径回填 publish_date
- 014005 数字 ID 解析：政府采购页特殊 URL 格式

**5. SmartScheduler 演进 (674 行)**
- Skip 重入队：跳过任务重新入队而非丢弃（修复 CCGP 详情页被永久跳过 bug）
- publish_date 兼容 datetime.date 入参
- per-source 全局锁修复（避免 1140 skipped 误跳）
- 真正并行：`schedule()` + max_requeue 限流
- 预排序 by recency

**6. Database 重大扩展 (950 行)**
- 新增 `upsert_projects_ccgp()` 方法（与 upsert_projects 并行）
- `upsert_projects` cols 列表加 `project_no`
- protected_cols：`full_content`/`content_preview` 不被空值覆盖
- 27 张表（含 2 张备份表 + schema_version + favorites_test）
- 关键表：projects_cqggzy (14109), projects_ccgp (58), vector_store (11369), keywords (29), audit_logs (490)

**7. 数据质量 (project_no 提取)**
- 4-22 时覆盖率 85.2% → 6-10 提升至 97.51% → 现 91.94%（因回填历史数据含更多低质量记录拉低均值）
- 9 类分类型覆盖：
  - 中标候选人公示/中标结果公示 100%
  - 答疑补遗 99.36%
  - 招标公告 91.6%
  - 采购结果公告 90.1%
  - 采购公告 88.4%
  - 变更公告 87.5%
  - 招标计划 62.8%（合规无编号）

**8. 内容质量**
- full_content 覆盖率：**97.44%** (13,748/14,109)
- content_preview 覆盖率：**97.44%** (13,748/14,109)
- Bug 2.1 已修：strip_title_dup 去除 content_preview 开头 title 重复
- 4 条 chrome 清洗规则加入 clean_text

### 📊 数据规模

| 时间 | 总数 | full_content | 项目编号 | 备注 |
|------|------|-------------|---------|------|
| 2026-04-22 | 5,848 | ~0 | 85.2% | 收尾时 |
| 2026-05-11 | 902 | ~30% | - | CCGP 集成期 |
| 2026-06-02 | 5,848 | 0% | - | 因保护字段回退 |
| 2026-06-10 | 12,061 | 88% | 97.51% | H1 修复后 |
| **2026-06-11** | **14,109** | **97.44%** | **91.94%** | **当前（含回填 9 段 +2,048）** |

### ⚠️ 当前活跃问题

1. **回填 10/20 段进行中**（2024Q1 ~ 2026Q2 × engineering/gov_purchase）
2. **data.html 工具栏**: 6 个筛选控件已加 ? tooltip
3. **多关键词 + 负关键词搜索**: 5-12 已实现（`search_parser`）
4. **导出端点**: `/api/projects` + `/api/export` 支持新语法
5. **favorites 空 title**: 已修复（c8df87c），回填脚本 d07bbf9

### 🟢 优点（验证）

1. **CQGGZY 单源架构稳定**：4 月以来 0 架构破坏性变更，仅增量修复
2. **数据回填机制成熟**：`scripts/backfill_segment.py` + `reprocess_pno_from_h1.py` 等 8+ 一次性脚本
3. **详情页鲁棒性增强**：H1 拼接 + URL 日期回填 + 数字 ID 解析 兜底所有边界
4. **DB schema 演进可控**：upsert cols 列表统一管理，protected_cols 保护重要字段

### 🔴 新增风险/待优化

1. **采集源单一**：CQGGZY 故障即全停，需考虑恢复 CCGP 或添加第三源
2. **回填脚本依赖主项目代码**：reprocess_pno_from_h1.py 用 requests+BS4 抓 Nuxt SSR，与 cqggzy.py 部分重复
3. **main.py 持续膨胀**：551 行包含 5 大职责（采集/调度/向量化/导出/统计），建议拆 run_collection_v2
4. **TenderInfo 25 字段 vs DB 实际**：db.py 中实际写入字段与 TenderInfo 定义需对齐审计

### 📈 进度

- 4-24 收尾：100% ✅
- **5-11 至 6-11 增量修复期：~50 commits, 0 架构破坏**
- **6-11 当前状态：生产可用，但仍在大量数据回填与质量修复期**

---

## 2026-04-19 18:45 更新

### Redis 去重集成 ✅ 完成

**实现内容：**
- `PriorityQueue._init_redis()`: 初始化 Redis 连接（支持失败回退）
- `PriorityQueue._check_url_exists()`: Redis+内存双重检查
- `PriorityQueue._add_url()`: 同步写入 Redis Set（7 天 TTL）
- `push()` 方法改造：使用新方法进行去重

**技术细节：**
- Redis URL: `redis://:infini_rag_flow@localhost:6379/0`
- Key: `tender:scheduler:seen_urls`
- TTL: 7 天
- 回退策略：Redis 不可用时自动使用内存去重

**测试状态：**
- [ ] Redis 连接测试
- [ ] 去重功能验证
- [ ] 重启后持久化验证

---

## 2026-06-16 12:30 重新评估

> 距 6-11 评估 +5 天。**核心架构重大演进：main.py 拆分 91% + 4 项数据质量修复；数据规模 14,109 → 108,409（7.7x）**。

### 🎯 关键里程碑（P0-4 main.py 拆分）

**PR #18 MERGED + 3 轮 hotfix + 部署**（6-15 19:46 起 24h 灰度监控）

| 阶段 | 改动 | 验证 |
|------|------|------|
| 拆分前 | main.py 551 行（5 大职责：采集/调度/向量化/导出/统计） | 难以维护 |
| 拆分后 | main.py **53 行**（-91%），3 模块（vectorize / scheduler / pipeline） | ✅ |
| Hotfix 1 | CrawlTask 漏 import | 部署失败 |
| Hotfix 2 | `os` 漏 import | 部署失败 |
| Hotfix 3 | `get_db` 漏 import | 部署失败 |
| 灰度验证 | 20:00 周期 16 min, **255/255 详情成功**, PG 552 条 | ✅ |

**教训（已写入 AGENTS.md）：** PURE LIFT 拆分需逐行检查 + 立即触发完整采集验证。

### 🟢 数据质量修复（4 项，6-1 ~ 6-15）

| 修复 | 影响 | 验证 |
|------|------|------|
| **Bad Nuxt URL** (v3) | 185 → 0 坏 URL；108,409 条全 trade/UUID 格式 | v2 失败回滚教训：先 SELECT 再 DELETE 坏记录 |
| **publish_date 提取** (v1+v2+v3) | 累计 **233 条**错位回填（例 id=612932: 7-1→6-9） | 3 层防御：标签优先 / 黑名单（10+20 字符）/ 范围 Y 过滤 / earliest + 兜底 created_at |
| **content_preview 清洗** | **15,466 条**更新（【】链 4,126→0；页脚备案号；信息时间变体） | 3 新规则加入 clean_text |
| **scraped_at NULL 大规模修复** | **10,331 条**回填（远超原估 600+） | backfill 脚本 upsert 缺字段蔓延 |

### 🔴 漏采集关键修复（4 项，6-3 ~ 6-5）

| Bug | 根因 | 修复 | 损失 |
|-----|------|------|------|
| **列表只采第 1 页** | main.py:156 未传 page_num | 新增 `_fetch_all_pages` 循环 | 6-1/6-2 漏 50+ 条/类 |
| **API edt 排他** | `edt=today` 漏当天 | `edt = today + timedelta(days=1)` | 每天 ~50 条 |
| **scraped_at NULL** | backfill 脚本 upsert 缺字段 | 强制传 scraped_at | 10,331 条 |
| **detail_limit 100 → 300** | 单周期只 100 条有详情正文 | `min(300, ...)` | 每周期 200+ 条 |

### 🟢 upsert 覆盖详情 bug 修复（6-5）

- **问题：** 列表 API 不返回 content → 列表 upsert 写空 fc → 覆盖详情阶段填的 fc
- **修复：** `protected_cols = {"full_content", "content_preview"}` 用 `COALESCE(NULLIF(EXCLUDED.x, ''), projects_cqggzy.x)`
- **验证：** 4 个补采脚本对 6-2~6-5 共 **488 条 100% 填上 fc, 0 cp=title**
- **附带：** `app/utils/filter.py:156` 移除 `title[:100]` 兜底（避免误导用户）

### 🟢 导出 CSV 端点 business_type 修复（6-5）

- **问题：** DB `business_type` 全 NULL → `/api/export/csv?category=政府采购` 返 0 行
- **根因：** 采集器从未写入；API 层序列化时根据 URL 推理，导出端点直查 DB
- **修复：** `app/api/routes/exports.py` 复用 URL 模式推理（`014005` / `order`）
- **验证：** 政府采购 5,001 行 / 工程招投标 4,673 行

### 📊 数据规模演进

| 时间 | projects_cqggzy | 备注 |
|------|-----------------|------|
| 2026-04-22 | 5,848 | 收尾 |
| 2026-06-11 | 14,109 | 上次评估 |
| **2026-06-15** | **108,409** | **当前（+694%，含 11 段回填 + URL 修复）** |

### 📈 单测覆盖

- 总数：**40 个**（26 + 14 新增）
- 通过率：40/40 ✅
- 新增覆盖：publish_date 提取 / Bad Nuxt URL 解析 / 详情页字段提取

### 🟡 当前活跃问题

1. **cp "无内容" 重抓** — 10,782 条 工程招投标+招标计划+pd<3-13（spike 验证：date=all 不可用，需 categorynum=014001019）
2. **fix/cqggzy-* 分支 4 commits 未推 origin** — 等用户拍板

### 🟢 优点（5 天验证）

1. **PURE LIFT 拆分**已 24h 灰度稳定，行为兼容 100%
2. **数据质量修复可追溯**：每条修复都有 backup 表 + 1 commit
3. **Bug 修复有防御深度**：protected_cols / API edt / backfill 强制 scraped_at 多层防护
4. **单测覆盖增长**：从 21 → 40（+90%）

### 🔴 新增风险

1. **回填脚本仍依赖主项目代码**（reprocess_pno_from_h1.py 用 requests+BS4，与 cqggzy.py 部分重复）
2. **Bad Nuxt URL 修复脚本 working tree**（scripts/fix_bad_nuxt_urls.py + fix_9_unmatched.py 未跟踪）
3. **scraped_at NULL 历史教训**：AGENTS.md 估 600+ 实际 10,331；规模估算应反复验证

### 📈 进度

- 4-24 收尾：100% ✅
- 6-11 上次评估：生产可用
- **6-16 当前：架构拆分完成，数据质量 5 项修复已部署，待 PR 推送**
